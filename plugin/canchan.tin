#class canchan open
#alias {cc.start} {
    set_last_quest cc;
    #class canchan.inner open;
    #ticker {canchan} {
        canchan;
    } {180};

    #action {你修行完毕} {
        #delay {wd.done} {
            chifan;
            canchan;
        } {0.1};
    };
    #class canchan.inner close;
    gt 禅院;
    on_there {
        canchan;
        show_to_quest 参禅;
    };
};

#alias {cc.stop} {
    unset_last_quest;
    #class canchan.inner kill;
    show_to_quest 参禅结束;
};

#class canchan close