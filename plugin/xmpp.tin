#class xmpp open
#var xmpp_listener milli;

#alias {xmpp.start} {
    #if {"${char_id}" == "${xmpp_listener}"} {
        #ruby load 'bin/xmpp.rb'
    };
};

#action {(%*)¸æËßÄã£ºnotify %*} {
    #var receiver %1;
    #var content %2;
    #ruby xmpp_send ${receiver} ${content}
} {1};

#alias {xmpp.notify} {
    tell ${xmpp_listener} notify %0;
};

#class xmpp close
