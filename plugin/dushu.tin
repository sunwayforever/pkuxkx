#class dushu open

#var ds_book shu;

#alias {ds.start} {
	#var ds_book %0;
	
	#class dushu.inner open;
	#if {"${ds_book}" == "shediao"}  {
		#alias {do_du} {
			du $ds_book 5;			
		};
	};
	#else {
		#alias {do_du} {
			du $ds_book for 5;
		};
	};
	
	#ticker {du} {
		do_du;
		#if {${nl_percent} < 20} {
			chifan;
			sleep;
		};
	} {2};

	#class dushu.inner close;

	show_to_quest ¶ÁÊé: ${ds_book};
};

#alias {ds.stop} {
	#class dushu.inner kill;
	show_to_quest ¶ÁÊé½áÊø;
};

#class dushu close